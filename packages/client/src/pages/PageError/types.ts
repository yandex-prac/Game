type PageErrorProps = {
  title: string
  subtitle: string
  href: string
}

type PageProps = {
  darkMode: boolean
}

type TitleProps = {
  darkMode: boolean
}

type SubTitleProps = {
  darkMode: boolean
}

type LinkProps = {
  darkMode: boolean
}

export type { PageErrorProps, PageProps, TitleProps, SubTitleProps, LinkProps }
