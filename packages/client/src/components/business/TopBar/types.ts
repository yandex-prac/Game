type TopBarProps = {
  darkMode: boolean
}

type WrapperProps = {
  darkMode: boolean
}

type ButtonProps = {
  darkMode: boolean
}

type IconProps = {
  darkMode: boolean
}

export type { TopBarProps, WrapperProps, ButtonProps, IconProps }
